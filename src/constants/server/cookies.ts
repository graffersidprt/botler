// current botler user token cookie name
export const CURRENT_BOTLER_USER_TOKEN_COOKIE_NAME = 'cbut';
// current botler user token cookie max age (30 minutes)
export const CURRENT_BOTLER_USER_TOKEN_COOKIE_MAX_AGE = 30 * 60 * 1000;

// the logged in user flag cookie name
export const RETURNING_USER_COOKIE_NAME = 'ru';
// the logged in user flag cookie max age (one year)
export const RETURNING_USER_COOKIE_MAX_AGE = 365 * 24 * 60 * 60 * 1000;
